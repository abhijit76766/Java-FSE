# LibraryManagementBoot

Spring Boot solution for Exercise 9.

## Endpoints

- `GET /books`
- `GET /books/{id}`
- `POST /books`
- `PUT /books/{id}`
- `DELETE /books/{id}`

Example POST body:

```json
{
  "title": "Effective Java",
  "author": "Joshua Bloch"
}
```
