# Exercise 15 - ticketraisingapp

`ComplaintRegister` component: controlled textbox (employee name) and
textarea (complaint). `handleSubmit` prevents default form submission,
generates a random reference number, and shows it in an alert.
