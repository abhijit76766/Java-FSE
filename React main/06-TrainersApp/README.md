# Exercise 6 - TrainersApp

React Router based SPA listing trainers (`/trainers`), a home page
(`/`), and a trainer detail page (`/trainers/:id`) that reads the id
via `useParams()` from `react-router-dom`.

Install: `npm install react-router-dom`
