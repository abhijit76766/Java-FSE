# Exercise 12 - ticketbookingapp

`App` keeps `isLoggedIn` in state. Guests see `GuestPage` (flight
listing only); once logged in, `UserPage` is shown (booking allowed).
Login/Logout buttons toggle the state and conditionally render the
right page/button.
