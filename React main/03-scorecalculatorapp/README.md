# Exercise 3 - scorecalculatorapp

Function component `CalculateScore` accepts Name, School, Total and
Goal as props and calculates + displays the average score. Styled with
a CSS file under Stylesheets/mystyle.css.
