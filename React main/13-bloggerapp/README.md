# Exercise 13 - bloggerapp

Three components (BookDetails, BlogDetails, CourseDetails) each
demonstrate a different conditional-rendering technique:
if/else return, ternary operator, and `&&` short-circuit. App.js
additionally uses an element variable (`content`) selected by
if/else-if to switch between the three, based on button clicks.
