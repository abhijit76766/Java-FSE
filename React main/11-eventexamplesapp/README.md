# Exercise 11 - eventexamplesapp

- Increment/Decrement counter buttons; Increment invokes two methods
  (increment the value + say hello with a static message).
- "Say Welcome" button invokes a function with `"welcome"` as an
  argument.
- A button that fires a synthetic `onPress`-style click event showing
  "I was clicked".
- `CurrencyConvertor` component: controlled input for rupees,
  `handleSubmit` handles the form submit event and converts to Euro.
