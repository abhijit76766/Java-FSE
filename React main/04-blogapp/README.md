# Exercise 4 - blogapp

`Post` class defines title/body properties. `Posts` class component
loads posts from https://jsonplaceholder.typicode.com/posts inside
`loadPosts()`, calls it from `componentDidMount()`, renders them, and
uses `componentDidCatch()` to show any rendering errors as an alert.
