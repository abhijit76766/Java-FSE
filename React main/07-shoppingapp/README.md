# Exercise 7 - shoppingapp

`Cart` class component has `Itemname` and `Price` properties (set from
props). `OnlineShopping` class component holds an array of 5 cart
items and loops through them, rendering a `Cart` row per item.
