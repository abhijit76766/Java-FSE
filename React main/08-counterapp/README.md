# Exercise 8 - counterapp

`CountPeople` class component stores `entrycount` and `exitcount` in
state (initialized in the constructor). `Login` button calls
`UpdateEntry()`, `Exit` button calls `UpdateExit()`.
