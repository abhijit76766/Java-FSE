# Library Management System

Linear search checks each book one by one and works on unsorted data.

Binary search repeatedly halves the search range but requires sorted data.

Time complexity:
- Linear search: `O(n)`.
- Binary search: `O(log n)`.

Use linear search for small or unsorted collections. Use binary search for large collections that can be kept sorted by title.

